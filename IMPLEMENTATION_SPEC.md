# 艶 II — 再現実装仕様書

文書版: 1.0

基準日: 2026-08-31

基準コミット: `4b33ebe` (`Add automatic subject-focused source analysis`)

現行ローカル版: `試作38 / mobile38`（作品ネイルを中央配置し、自由制作入口へ淡い発光とタップ粒子を追加）

対象: `tsuya-v2-deploy` の現行統合作品

## 0. この文書の目的

この文書は、別のAIや開発者が「艶 II」を作り直す場合に、現在の基準版と同じ体験・画面遷移・機能・制約を再現するための実装仕様書である。

単なるネイル試着アプリではなく、鑑賞者自身の手から作品世界へ入るWeb作品として再現すること。見た目だけ似せ、作品と試着とSOURCEの循環を省略したものは同等実装とみなさない。

実装前に、必ず次を全文読む。

1. Vaultルートの `AGENTS.md`
2. このプロジェクトの `AGENTS.md`
3. `PROJECT_STATUS.md`
4. 本書 `IMPLEMENTATION_SPEC.md`
5. 現行の `experience-prototype/index.html`

仕様が食い違う場合の優先順位は次の通り。

1. 利用者の最新の明示指示
2. Vaultルートの `AGENTS.md`
3. プロジェクトの `AGENTS.md`
4. 現行コードと `PROJECT_STATUS.md`
5. 本書

本書は、基準コミット `4b33ebe` の実装を文章化したものである。勝手な改善、整理、現代化、フレームワーク移行は行わない。

---

## 1. 作品の核

### 1.1 目的

鑑賞者の手を作品世界への入口にする。基本の体験順序は次の通り。

`鑑賞者の手 → 色 → 表面 → 形 → 光 → 痕跡 → 各作品`

利用者は作品を見るだけでなく、自分の手に作品由来の5本セットを装着し、その奥にある作品映像へ移動する。また、身のまわりの写真やカメラ映像から色・線・光・形を採集し、自分の5本セットを作る。

### 1.2 成立させる関係

- 作品を見る → 試着する、だけにしない。
- 試着する → 体験の奥に作品がある、という順序も成立させる。
- `作品 → 作品ネイル → SOURCE → 自分の手 → 元作品` を一つの循環として扱う。
- TRACE / SKINは最上位分類にせず、試着の奥にある作品レイヤーとして残す。

### 1.3 デザイン上の禁止事項

- 美容EC、商品カタログ、一般的なネイルサロン風にしない。
- 入口から説明過多・難解な現代美術サイトにしない。
- 過剰な3D、意味のないスクロール演出、派手すぎるアニメーションを追加しない。
- 作品映像を暗いオーバーレイで見えにくくしない。
- 10作品を削除、統合、改題、並べ替えしない。
- 作品別5本セットを全指同じ柄にしない。
- SOURCE画像を爪へ直接貼り付けない。
- SOURCE画像を外部APIや生成AIへ送らない。

---

## 2. 対象ページとURL仕様

### 2.1 現行ページ

| 役割 | ファイル | URL例 |
|---|---|---|
| 作品の入口 | `top-prototype/index.html` | `/top-prototype/` |
| 統合作品本体 | `experience-prototype/index.html` | `/experience-prototype/` |
| 作品ページ | 同上 | `?work=COLOR` |
| 作品別固定試着 | 同上 | `?try=COLOR` |
| 自由制作 / SOURCE | 同上 | `?mode=custom` |
| 作品連携SOURCE | 同上 | `?mode=custom&from=COLOR` |

リポジトリ直下の `index.html` と `top.html` は旧ページであり、現行統合作品の本体ではない。整理目的でも削除・置換しない。

### 2.2 URLパラメータ

- `work`: 作品キー。作品詳細ページを表示する。
- `try`: 作品キー。作品プリセット固定の試着開始画面を表示する。
- `mode=custom`: 自由制作 / SOURCEを表示する。
- `from`: CUSTOMへ引き継ぐ元作品キー。
- `v`: キャッシュ回避用の内部版。試作38は `mobile38`。

作品キーは `COLOR / SURFACE / SCULPT / LIGHT / HAND / TRACE / SPACE / THINKING / ORB / FRAME`。旧互換として `NAILORB` は `ORB` に正規化する。

### 2.3 戻る動作

- 作品ページの戻る: 作品一覧へ戻る。
- 固定試着のBACK: 元作品へ戻る。
- `?mode=custom&from=作品キー` のBACK: 元作品へ戻る。
- `?mode=custom` のBACK: 作品一覧へ戻る。
- HOME: 常に `top-prototype/` へ戻る。
- CUSTOMのBACKはブラウザ履歴に依存せず、明示URLへ遷移する。

---

## 3. 技術構成

### 3.1 必須構成

- 静的HTML / CSS / JavaScript。
- ビルド工程なし。
- npm依存、フレームワーク、サーバー側処理なし。
- GitHub Pagesのリポジトリルート配信で動作する相対パスにする。
- 手の追跡はMediaPipe Hands `0.4.1675469240`。
- 読み込み元は `https://cdn.jsdelivr.net/npm/@mediapipe/hands@0.4.1675469240/`。
- 描画は2D Canvas。WebGL、3D手モデルは使わない。
- 入力画像の分析はブラウザ内Canvasのみで完結させる。

### 3.2 カメラ設定

```js
navigator.mediaDevices.getUserMedia({
  video: {
    facingMode: facing,
    width: { ideal: 1280 },
    height: { ideal: 720 }
  },
  audio: false
});
```

- 初期値は `facing = 'environment'`、つまり背面カメラ。
- 切替ボタンで `environment` と `user` を切り替える。
- 前面カメラのときだけ描画座標を左右反転する。

### 3.3 MediaPipe設定

```js
{
  maxNumHands: 1,
  modelComplexity: 0,
  minDetectionConfidence: 0.5,
  minTrackingConfidence: 0.3
}
```

この値はiPhone実機での軽さと追随性を優先して確定した値である。許可なく変更しない。

### 3.4 実装状態

```text
idle   : カメラ開始前
live   : カメラ映像と手追跡を連続描画
frozen : 撮影したフレームを固定表示
```

---

## 4. 入口ページ `top-prototype/`

### 4.1 見た目

- 全画面、スクロールなし。
- 背景は白からベージュの放射グラデーション。
- 中央に明朝体の「艶」をCanvas描画。
- 上部に `HAND & LIGHT / 2026`。
- 下部に短い線と `触れて、作品へ`。
- セーフエリアを考慮する。

### 4.2 「艶」の動き

- 文字は銅・ブロンズ系グラデーション。
- 文字内部をマスクとして使い、光沢が横切る。
- 登場時は下から滑らかに現れる。
- 現行の `variant` 初期値は `0`。別案の粒子表現コードが残っていても、初期表示を勝手に変えない。
- 通常クリックまたはキーボード操作ではアンカーの即時遷移を止め、`go()`を開始する。別タブで開く修飾キー付きクリックはブラウザ標準動作を維持する。
- タップ後は約900msで退場し、約420ms後からベージュのveilを出す。
- 退場完了後、アンカーの `../experience-prototype/?v=mobile38` へ遷移する。

### 4.3 アクセシビリティ

- 画面全体をリンクとして扱う。
- `aria-label="触れて作品へ"` を持たせる。
- キーボードフォーカス時は控えめなアウトラインを表示する。

---

## 5. 統合作品本体の画面状態

### 5.1 作品一覧 / HUB

パラメータなしで表示する。

- 10作品の映像を背景で約5.2秒ごとに切り替える。
- 映像は約1.9秒のクロスフェードでつなぐ。
- 画面が非表示の間は切替タイマーを止め、復帰時に再開する。
- `prefers-reduced-motion: reduce` の場合、自動切替を行わない。
- 現在の作品番号、キー、題名、ENTERを表示する。
- 01〜10のボタンから各作品へ直接進める。
- 作品順は `works` オブジェクトの定義順を正本とする。

### 5.2 作品ページ `?work=KEY`

- 作品映像、作品番号、キー、題名、5本ネイル見本を表示する。
- 10作品とも、題名下の説明文は表示しない。説明文データは将来の再利用に備えて保持してよいが、作品ページと作品オーバーレイの画面へ出さない。
- 作品映像は `object-fit: contain`。
- 映像へ暗いフィルターをかけない。
- 映像の元の縦横比と明るさを優先する。
- スマホでは作品映像領域を画面高の50%にする。題名は25〜34pxに抑え、5本のネイル見本は幅38pxを基準に拡大する。
- 5本の見本は全指を同じ長さにせず、親指・小指、中指、その他の順で高さを変える。
- 10作品を横方向のスイッチャーで切り替えられる。
- スマホでは選択中ボタンを中央付近へスクロールする。
- `TRY ON` で `?try=現在作品` へ進む。
- `じぶんでつくる` で `?mode=custom&from=現在作品` へ進む。
- 作品ネイル5本は、スマホの作品ページでは画面中央へ配置する。
- `じぶんでつくる`は淡い水色の発光と星印で、制作世界への入口として区別する。
- `じぶんでつくる`をタップすると16個の小さな粒子を約480msだけ放ち、180ms後から退場フェード、460ms後にCUSTOMへ遷移する。
- `prefers-reduced-motion: reduce`の場合は発光アニメーションと粒子を止め、導線自体は維持する。

### 5.3 作品別固定試着 `?try=KEY`

- 指定作品の5本セットを起動時に適用する。
- START CAMERAでカメラを開始する。
- 作品名、作品番号、5本見本を表示する。
- デザイン、カラー、質感、スカルプ、光の変更パネルは表示しない。
- 試着中に右上の作品シグナルを押すと、対応作品を開ける。
- BACKとWORKは元作品へ戻る。
- 作品プリセットの柄、色、形、長さ、軌跡設定を変更しない。

### 5.4 自由制作 / SOURCE `?mode=custom`

- SOURCE入口の背景動画は `source-assets/source-process-loop-v1.mp4`。これは暫定素材である。
- `カメラから作る` と `写真から作る` を表示する。
- 写真選択後は、カメラ開始前でもSOURCE設計画面へ入れる。
- 写真選択後、右上の小さなプレビューへ元画像を切らずに表示する。
- 右上プレビューは `object-fit: contain`。
- SOURCE設計画面の上部に大きな元画像を再掲しない。
- SOURCE設計中、5本セット見本を中心に見せる。
- `TRY ON` は画面下部で押せる状態にする。
- TRY ON後はパネルを小さな `DESIGN` バーへ畳む。
- DESIGNを押すとパネルを開き、表示は `← TRY ON` に変わる。
- 別案を選択したら試着画面へ戻し、パネルを再び畳む。

### 5.5 作品連携SOURCE `?mode=custom&from=KEY`

- 元作品の番号、キー、題名、5本プリセットを引き継ぐ。
- `FROM ARTWORK` を表示する。
- `作品ネイル` ボタンで元作品プリセットへ戻せる。
- SOURCEで別案を作った後もBACKで元作品へ戻れる。

---

## 6. 登録作品と固定プリセット

順番、キー、題名、映像、プリセットは次を正本とする。`looks` と `colors` は親指から小指までの順。

### 01 COLOR / 色

- 映像: `art-assets/color-pattern.mp4`
- looks: `artColor, sheer, nuance, artColor, flash`
- colors: `#d14f78, #477d6c, #e0b936, #795a9f, #c55d3d`
- tune: `gloss 76 / opacity 98 / nailSize 98 / sculptLen 138 / natural`
- vars: `sheer .38 / flash .44`
- trail: OFF / 0

### 02 SURFACE / 表面

- 映像: `art-assets/surface-gold.mp4`
- looks: `artSurface, sheer, chrome, artSurface, frenchmirror`
- colors: `#8a5d25, #c18d3f, #1f7772, #d2aa63, #a97737`
- tune: `gloss 100 / opacity 100 / nailSize 100 / sculptLen 144 / round2`
- vars: `sheer .42 / frenchmirror .22`
- trail: OFF / 0

### 03 SCULPT / FORMWORK

- 映像: `art-assets/sculpt-wire.mp4`
- looks: `artSculpt, milk, line, artSculpt, frenchmirror`
- colors: `#e8ddd0, #d4c4b3, #f0e7dd, #b8d9d2, #e5d6c5`
- tune: `gloss 42 / opacity 88 / nailSize 94 / sculptLen 112 / natural`
- vars: `milk .56 / line .58 / frenchmirror .18`
- trail: OFF / 0

### 04 LIGHT / 光

- 映像: `art-assets/light-mandala.mp4`
- looks: `mizuhikari, stardust, aurora, artLight, flash`
- colors: `#173f4d, #202637, #447c86, #9c6681, #be4f77`
- tune: `gloss 100 / opacity 96 / nailSize 102 / sculptLen 156 / almond`
- vars: `mizuhikari .68 / stardust .72 / aurora .68 / flash .5`
- trail: ON / 88 / `#b9e8df`

### 05 HAND / 手

- 映像: `art-assets/hand-drawing.mp4`
- looks: `artHand, sheer, line, artHand, french`
- colors: `#b89c91, #e7d8cb, #6d5660, #8e7770, #314b6b`
- tune: `gloss 30 / opacity 82 / nailSize 96 / sculptLen 124 / natural`
- vars: `sheer .32 / line .2 / french .18`
- trail: OFF / 0

### 06 TRACE / 痕跡

- 映像: `art-assets/trace-dust.mp4`
- looks: `artTrace, matte, sheer, artTrace, stardust`
- colors: `#313236, #202126, #b9b8b2, #777a80, #d5d4cf`
- tune: `gloss 12 / opacity 88 / nailSize 98 / sculptLen 132 / round2`
- vars: `sheer .2 / stardust .42`
- trail: ON / 42 / `#d8d6cf`

### 07 SPACE / 宇宙と手

- 映像: `art-assets/space-hand.mp4`
- looks: `artSpace, stardust, mizuhikari, artSpace, chrome`
- colors: `#0d1934, #c9a54c, #dce8df, #4a335f, #8e743d`
- tune: `gloss 96 / opacity 96 / nailSize 102 / sculptLen 152 / almond`
- vars: `stardust .76 / mizuhikari .58`
- trail: ON / 72 / `#e5cf89`

### 08 THINKING / 思惟する手

- 映像: `art-assets/thinking-hand.mp4`
- looks: `artThinking, sheer, line, artThinking, matte`
- colors: `#d7c5bf, #e9ddd6, #8c5d67, #b8a09c, #4a4448`
- tune: `gloss 24 / opacity 84 / nailSize 96 / sculptLen 120 / natural`
- vars: `sheer .28 / line .36`
- trail: OFF / 0

### 09 ORB / White Orb

- 映像: `art-assets/nail-white-orb.mp4`
- looks: `artOrb, milk, flash, artOrb, french`
- colors: `#c52d35, #f2eee5, #d94f62, #8f1827, #b92734`
- tune: `gloss 100 / opacity 100 / nailSize 104 / sculptLen 166 / round2`
- vars: `milk .62 / flash .64 / french .25`
- trail: OFF / 0

### 10 FRAME / 二つの手の輪舞曲

- 映像: `art-assets/frame-0001-0240.mp4`
- looks: `artFrame, sheer, frenchmirror, artFrame, aurora`
- colors: `#e2b9b2, #ead8cf, #caa75d, #f0cbc6, #d7cabd`
- tune: `gloss 82 / opacity 90 / nailSize 100 / sculptLen 148 / coffin`
- vars: `sheer .3 / frenchmirror .16 / aurora .38`
- trail: ON / 34 / `#f0d4c8`

各作品の説明文データは `works` オブジェクトの `text` を正本として保持するが、試作32では画面へ表示しない。要約やAIによる書き換えも行わない。

---

## 7. 手追跡と爪描画

### 7.1 ランドマーク

指ごとに次の2点を使う。

```js
const FINGERS = [[4,3], [8,7], [12,11], [16,15], [20,19]];
```

順に親指、人差し指、中指、薬指、小指。1点目が指先、2点目がDIP側。

### 7.2 手の大きさの基準

```js
const PALM_BONES = [[0,5], [0,17], [5,17], [0,9], [0,13], [9,17]];
const MIN_LEN_RATIO = [0.240, 0.173, 0.188, 0.173, 0.143];
```

手のひらの骨のうち最長の画面距離を `handRef` とし、指節が奥行き方向につぶれたときも爪が消えない下限として使う。

### 7.3 平滑化

- 座標ごとに速度を持たせた既存の平滑化を維持する。
- 微小変化 `0.0030` 未満では速度を半減し、座標更新を抑える。
- 速度側cutoffは `1.0`。
- 位置側cutoffは `0.55 + 32 * abs(velocity)`。
- 手を見失って280msを超えたら平滑化配列を空にする。
- 追跡・平滑化・爪座標をUI改修と同時にリファクタリングしない。

### 7.4 爪サイズと位置

```text
rawLen  = max(指先とDIPの距離, handRef × 指別下限)
lenPct  = sculptLen / 100
width   = len × (0.54 - 0.06 × (lenPct - 1)) × nailSize/100
height  = len × 0.94 × nailSize/100 × lenPct
pushOut = len × (0.10 + 0.30 × (lenPct - 1))
```

- 爪の中心を指先から指方向へ `pushOut` だけ移動する。
- 指方向に対して `+ π/2` 回転して描画する。
- 長さと角度には追加の状態平滑化をかける。
- 形は `natural / round2 / almond / square / coffin` の5種。
- 爪本体、縁の陰影、光沢の順に描く。
- マット系の柄には共通光沢を重ねない。

### 7.5 肌表示

```js
const SKIN_FX = { div: 5, a: 0.42, lift: 0.45 };
```

- カメラ映像を縮小して戻す軽量処理で肌だけをわずかに柔らかくする。
- 爪はその後に描き、鮮明さを保つ。
- 手の周囲だけを白くする、背景だけをぼかす等の加工は採用しない。

---

## 8. SOURCE解析仕様

### 8.1 原則

- 画像生成AIを使わない。
- 入力画像を外部へ送らない。
- 写真そのものを爪へ貼らない。
- 色、線、密度、明暗、光、方向、特徴点だけを抽出する。
- 抽出値を作者が定めた5本構成へ渡す。

### 8.2 入力

- `写真を選ぶ`: 端末内の画像ファイル。
- `いま映っているもの`: 試着中の現在カメラフレーム。
- 読み込み画像の最大辺は1400pxへ縮小する。
- EXIF方向は `createImageBitmap(..., {imageOrientation:'from-image'})` を優先する。

### 8.3 分析用Canvas

- 入力を160×160の正方形Canvasへ中央 `cover` で切り取る。
- このCanvasは分析用であり、表示用画像と混同しない。
- 表示用プレビューは元の縦横比を保ち `contain` で描く。

### 8.4 自動背景抑制

`applyAutomaticSubjectMask()` を使う。

1. 外周約9%の領域を2px刻みで読む。
2. RGBを16段階相当で量子化し、頻度上位8色を背景候補にする。
3. 各画素について背景色との距離を計算する。
4. 中央優先度を加え、外周ペナルティを加える。
5. 5×5近傍で重みを平滑化する。
6. 重みをアルファ値へ変換する。
7. 有効画素が全体の12%未満なら、中央域を残すフォールバックを使う。

追加の被写体タップや手動切り抜きを要求しない。

### 8.5 抽出結果

- パレット: 最大8色。
- 線方向: 輝度勾配から角度を算出。
- 線密度: 輪郭強度を正規化。
- コントラスト: 輝度分散から算出。
- 光量: 高輝度画素の重み付き割合。
- 彩度: RGB最大・最小差から算出。
- 特徴点: 強い輪郭を最大42点。
- 色が不足する場合は既定のニュートラル色を補う。

### 8.6 生成方式と3案

各方式は必ず3案を生成する。SOURCEのMIX表示名は `流転 / 景層 / 残光`。

| 方式 | 1案目 | 2案目 | 3案目 |
|---|---|---|---|
| COLOR | CHROMA | TONE | QUIET |
| PATTERN | 流転 | 景層 | TRACE |
| LIGHT | 残光 | HALO | FLASH |
| MIX | 流転 | 景層 | 残光 |

生成する5本は、主役、背景、抜け、質感、ポイントの役割を分ける。同じ柄を5本へ複製しない。

### 8.7 画像由来の再現可能な揺らぎ

色の得点は `色の量 ×（基礎値 + 彩度 × 彩度倍率）`。固定値 `0.28 / 2.8`を唯一の正解とは扱わず、画像ごとに次の順で小さく変える。

1. `sourceSeedFromPixels()` が分析用画像の色と透明度を順番に読み、画像ごとの整数を作る。
2. `sourcePaletteTunings()` がその整数から0〜1の数を決まった順序で作る。
3. その数を次の上限・下限へ入れ、3案の基礎値と彩度倍率を決める。

| 案 | 基礎値 | 彩度倍率 |
|---|---:|---:|
| 流転 | 0.25〜0.31 | 2.75〜3.15 |
| 景層 | 0.30〜0.36 | 2.20〜2.65 |
| 残光 | 0.22〜0.28 | 3.05〜3.45 |

- 同じ画像では同じ整数になり、ページを開き直しても同じ3案を再現する。
- 別の画像では整数が変わり、3案の数値も少し変わる。
- 変えるのは色を選ぶ強さだけ。5本の模様順、爪の形と長さ、線・形・光の描画、MediaPipeの手追跡は変更しない。
- 係数が変わっても得点順位が同じ場合、8色の並びが変わらないことは正常である。

### 8.8 生成時の共通設定

- `gloss`: PATTERNのみ72、それ以外96。
- `opacity`: 96。
- `nailSize`: 95。
- `sculptLen`: 118。
- `sculptShape`: `natural`。
- パレット色は案番号ごとに位置をずらし、5本へ異なる順で割り当てる。

---

## 9. 自由制作機能

固定試着では非表示、CUSTOMでのみ使用可能にする。

### 9.1 デザイン

- 指ごと、または5本一括で編集できる。
- 既存の柄一覧を維持する。
- 柄によっては強さをスライダーで調整できる。
- 作品専用描画キーと通常デザインキーの両方を壊さない。

### 9.2 カラー

- 指ごと、または5本一括で色を適用する。
- 標準パレット、濃淡、任意カラー、写真から配色を維持する。
- SOURCE由来のパレットも同じカラーUIへ反映する。

### 9.3 質感

- ツヤ: 10 / 65 / 100。
- 濃さ: 65 / 96 / 100。
- 大きさ: 95 / 100 / 120。

### 9.4 スカルプ

- 長さ: 100〜220。
- 形: `natural / round2 / almond / square / coffin`。

### 9.5 光

- 軌跡ON/OFF。
- 強さ0〜100。
- 色はネイル色追従、クリーム、ローズ、セージ、ラベンダー。
- 軌跡寿命は620ms、最大420点。
- 採用済み作品プリセットの軌跡設定は変更しない。

### 9.6 手描きエディタ

- ペン、細ペン、エアブラシ、消しゴム。
- 全消し。
- 線幅3〜46。
- 完了した柄を爪へ適用できる。
- 保存キーは `tsuya.designs`。

### 9.7 自分のデザインを作品として見る

- CUSTOM内の `作品として見る` から全画面展示を開く。
- 展示は `CHIPS`のみとし、現在の5本を独立したネイルチップとして暗い展示空間に並べる。
- チップ5本は画面下へ寄せず、題名と下部操作の間にある展示領域の中央付近へ置く。
- 手やマネキンへ装着する`HAND`表示は設けない。
- GLB、WebGL、外部3Dライブラリは使わず、既存の2D Canvas描画を再利用する。
- 現在選択中の5柄、5色、濃さ、艶、長さ、形、柄パラメータをそのまま反映する。
- 展示画像内に `HAND & LIGHT / PERSONAL WORK`、`MY TRACE`、`FIVE NAIL STUDY / 2026` を入れる。
- `画像を保存` で `tsuya-my-trace.jpg` を共有または保存できる。
- BACKで自由制作の状態を保ったまま戻る。
- 展示画面は作品別固定試着には出さない。

---

## 10. 撮影・比較・保存

### 10.1 撮影

- シャッターで現在のCanvasを固定する。
- 3秒セルフタイマーを提供する。
- 撮影後は `写真に保存` と `もう一度` を表示する。
- 撮影画像は自動的に比較欄へ追加する。

### 10.2 比較

- 保存キー: `tsuya.shots`。
- 最大4枚。
- 新しい画像を先頭へ追加する。
- 保存画像の最大幅は560px、JPEG品質0.7。
- 各画像から「この設定に戻す」が使える。
- 個別削除ができる。
- localStorage容量不足時は古い画像から減らして再試行する。

### 10.3 写真保存

- CanvasをJPEG品質0.92で出力する。
- Web Share APIでファイル共有可能なら `tsuya.jpg` を共有する。
- タッチ端末で共有できない場合は画像を全画面表示し、長押し保存を案内する。
- デスクトップでは `tsuya.jpg` をダウンロードする。
- 撮影直後に「アプリ内の比べるへ保存済み」と「端末保存は別操作」を分けて表示する。
- 端末保存の開始、完了、中止、失敗を同じ表示欄で言い分ける。
- Web側から写真アプリへの保存成否を直接取得できないため、保存操作後は写真アプリで確認するよう案内する。
- 長押し保存用の全画面には、上部の `← 試着へ戻る` と下部の `保存画面を閉じて試着へ戻る` を常に表示する。

### 10.4 ネイルセット保存

- 保存キー: `tsuya.sets`。
- 最大20件。
- 名前、5本の柄、5色、質感、形、柄パラメータを保存する。
- 保存セットは再適用・削除できる。

---

## 11. UI・表示基準

### 11.1 最優先端末

- iPhone Safariを最優先する。
- 基準画面は390×844相当。
- `100svh` と `env(safe-area-inset-*)` を考慮する。
- デスクトップだけ整えて完了にしない。

### 11.2 言葉

- UIは短い言葉と視覚的な流れで誘導する。
- 小さな日本語説明を増やしすぎない。
- 主要語は `TRY ON / SOURCE / DESIGN / BACK / HOME / WORK / WORKS`。
- 作品ページの自由制作入口は `じぶんでつくる`。

### 11.3 タップ表示

- すべての主なタップ位置に短い視覚フィードバックを出す。
- 白いリングと金色の中心点。
- 表示時間は約0.42秒。安全のため700ms後にも削除する。
- 画面収録時にもタップ場所が分かること。

### 11.4 重要な配置条件

- CUSTOMのSOURCE設計中、TRY ONを画面下部へ固定する。
- 390×844でTRY ONが画面外や別要素の背後へ隠れない。
- 試着中のパネルは畳めるが、機能を削除しない。
- BACKとHOMEは全画面状態から到達できる。
- 選択画像プレビューは右上で切らない。

---

## 12. 変更禁止領域

利用者の明示許可なしに、次を変更しない。

- MediaPipeの版とCDN。
- `makeHands()`、`ensureLive()`、`updateLandmarks()`、`startCamera()`、`startLoops()`。
- ランドマーク平滑化、爪座標、片手追跡の数値。
- 作品別固定試着と自由制作の分離。
- 10作品の数、順番、キー、題名。
- 採用済みの5本プリセット、形、長さ、軌跡。
- 作品映像の明るさ、縦横比、`object-fit: contain`。
- SOURCE画像を外部へ送らない方針。
- 自動背景抑制と、追加操作を要求しない方針。
- CUSTOMの明示的な戻り先、HOME、タップ表示。
- 旧ページと元動画。

変更が必要な場合は、変更理由、影響する画面、実機で再確認が必要な項目を先に利用者へ説明し、許可を得る。

---

## 13. 実装完了の受け入れ条件

### 13.1 ローカル起動

リポジトリ直下で次を実行する。

```bash
python3 -m http.server 8765 --bind 127.0.0.1
```

### 13.2 ブラウザ確認

- 390×844でトップの「艶」から作品一覧へ進める。
- 作品一覧で10作品が定義順に表示される。
- 背景映像が約5.2秒ごとに切り替わる。
- 10作品すべての映像、題名、5本見本を表示でき、題名下の説明文は表示されない。
- 各作品から固定試着へ進める。
- 固定試着にカスタムパネルが出ない。
- 作品ごとに正しい5本プリセットが適用される。
- 作品ページからSOURCEへ入り、元作品を引き継げる。
- 直接CUSTOMへ入った場合は作品一覧へ戻る。
- 写真選択後、右上プレビューが切れない。
- SOURCEで最大8色と線・光・方向の数値が出る。
- 4方式それぞれで3案が生成される。
- 背景のある写真で外周背景が配色へ入りにくい。
- SOURCEのTRY ONが画面内にある。
- `作品として見る`でCHIPS展示を開ける。
- 5本の現在デザインが両方の展示へ反映される。
- 展示のBACKで自由制作へ戻れる。
- 展示画像を `tsuya-my-trace.jpg` として共有または保存できる。
- 試着開始時にパネルがDESIGNへ畳まれる。
- DESIGNを開閉できる。
- BACKとHOMEが正しい行き先を指す。
- タップ位置にリングと金色中心点が出る。
- コンソールに新しいerror/warnがない。
- JavaScript文法検査と `git diff --check` が成功する。

### 13.3 iPhone Safari実機確認

- カメラ許可。
- 背面・前面切替。
- 片手追跡と5本の爪追随。
- 爪が指先をカメラへ向けても消えにくい。
- カメラ映像からSOURCE生成。
- 3秒タイマー、撮影、比較、再適用。
- 共有シートまたは長押しによる写真保存。
- 動画自動再生。
- セーフエリア、固定UI、スクロール。
- 10作品の固定プリセット。

カメラのない自動試験では、実機項目を成功扱いにせず「要確認」と記録する。

---

## 14. 同等実装とみなさない例

- 1本だけのネイルを5本へ複製したもの。
- 写真をそのまま爪形へクリップしたもの。
- 生成AI APIへ写真を送るもの。
- 作品ページとネイル試着が別々で循環しないもの。
- 固定試着でも色や柄を自由変更できるもの。
- CUSTOMで元作品へ戻れないもの。
- 作品映像をcoverで大きく切り、暗くしたもの。
- デスクトップでは動くがiPhoneでTRY ONやBACKが隠れるもの。
- MediaPipeや爪座標を別方式へ作り替え、追随感が変わったもの。
- 10作品の一部を省略したもの。

---

## 15. 現在の未完成項目

次は仕様未確定であり、別のAIが先回りして決めない。

- SOURCE入口の暫定動画の最終差し替え。
- 自由制作専用ページの追加編集機能。
- 各作品の題名、フォント、素材の最終校正。
- 作品ごとの専用詳細ページをさらに発展させるかどうか。
- 細い被写体、画面端の被写体、背景と同色の被写体に対する背景抑制精度。
- 最新iPhoneでのカメラ由来SOURCE全回帰。

これらは利用者の判断を待つ。

---

## 16. 作業と引継ぎのルール

- 変更前に `experience-prototype/index.html` の該当部分を読む。
- 必要最小限の差分にする。
- UI変更と追跡中核のリファクタリングを同時に行わない。
- `git add .` を使わず、対象ファイルを明示する。
- `._*` と `.DS_Store` を読まない、追加しない、成果物にしない。
- 変更後は `PROJECT_STATUS.md` に事実だけを追記する。
- 未確認事項は「要確認」と書く。
- 公開GitHubへのpushは利用者の明示承認がある場合だけ行う。
- 制作情報を含む引継文書を、許可なく公開リポジトリへ送らない。

以上を満たし、基準版と同じ動作を保つこと。
