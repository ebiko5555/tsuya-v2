# 艶 II — 制作継続ルール

このファイルは `tsuya-v2-deploy` 固有の引継ルールである。上位Vaultの
`/Volumes/blender/新Secondbrain/AGENTS.md` と併せて守ること。

## 1. 作品の目的とコンセプト

「艶 II」は、ネイル試着アプリを作品サイトの付属機能として置くのではなく、鑑賞者自身の手を作品世界への入口にするWeb作品である。

体験の基本構造は次の通り。

`鑑賞者の手 → 色 → 表面 → 形 → 光 → 痕跡 → 各作品`

鑑賞者は一見すると洗練されたネイル試着アプリへ入り、自分の手に作品由来のネイルを装着する。その後、色・表面・形・光・手・痕跡などの作品へ進み、さらに身のまわりの写真やカメラ映像から自分の5本セットを作る。目標は「作品を見る → 試着する」ではなく、`試着する → 体験の奥に作品世界がある`という関係を成立させること。

TRACE / SKINの世界観は捨てず、サイト最上位の分類ではなく、試着体験の奥にある作品レイヤーとして扱う。

## 2. 絶対に守るデザイン方針

- スマートフォン、特にiPhone Safariを最優先する。デスクトップ版だけ整えて完了としない。
- 美容EC、一般的なネイルサロン、商品カタログの見た目にしない。
- 入口から難解すぎる現代美術サイトにもせず、操作を続けるうちに作品空間へ移る構成にする。
- UIは短い言葉と視覚的な流れで誘導する。説明的な日本語や小さなラベルを増やしすぎない。
- 過剰な3D、意味のないスクロール演出、派手すぎるアニメーションは追加しない。
- 作品映像を暗くして見えにくくしない。作品ページでは元の縦横比と明るさを優先する。
- 現在登録されている10作品を、明示指示なしに減らさない。
- 作品別ネイルは5本すべて同じ柄にしない。主役、ベース、抜け、質感、ポイントの役割を分け、通常のサロンデザインとしても完成度のあるセットにする。
- 作品別試着は作品プリセットを固定し、色や柄を変更するパネルを出さない。自由制作は別のCUSTOM/SOURCEルートで行う。
- 自由制作には、指ごとの柄・色指定、既存デザイン、画像柄、手描き、配色、濃淡、ツヤ、濃さ、大きさ、長さ、形、光の軌跡、セット保存が既にある。UI整理を理由にこれらを削らない。
- SOURCEは「画像を爪へ貼る」機能ではない。画像から色、線、密度、明暗、光、形を採集し、作者が定めた5本構成へ再編成する。
- SOURCEへ入れた写真は外部へ送らず、端末内のCanvas処理だけで分析する。生成AIのAPIへ画像を送る機能へ勝手に置き換えない。
- 写真分析では、追加操作を要求せず、外周の背景候補を自動的に弱めて中央の被写体を優先する現行方式を維持する。
- 選択画像の表示は切らない。右上プレビューは `object-fit: contain`。SOURCE設計画面の上部に大きな画像を再掲しない。
- TRY ONはSOURCE設計画面の下部で押せる状態を維持する。試着中は操作パネルを小さな`DESIGN`バーへ畳み、必要なときだけ開ける。
- 全画面から戻り先が分かる導線を残す。CUSTOMの`BACK`は、作品経由なら元作品、直接訪問なら作品一覧へ戻る。`HOME`はトップページへ戻る。
- 画面タップ位置を示す白いリング＋金色の中心点のフィードバックを維持する。

## 3. 技術構成

- 静的HTML/CSS/JavaScript。ビルド工程、フレームワーク、npm依存はない。
- GitHub Pagesで `main` ブランチのリポジトリルートを公開している。
- 手の追跡: MediaPipe Hands `0.4.1675469240` を jsDelivrから読み込む。
- カメラ: `navigator.mediaDevices.getUserMedia()`。初期設定は背面カメラ、理想解像度1280×720。
- 追跡設定: `maxNumHands: 1`、`modelComplexity: 0`、`minDetectionConfidence: 0.5`、`minTrackingConfidence: 0.3`。
- 描画: 2D Canvas。WebGLや3D手モデルは使わない。
- ネイル配置: MediaPipeのランドマークを平滑化し、各指の2点を基準に2D描画する既存方式。
- 写真解析: Canvas上で色、輝度、輪郭、方向、密度を数値化する決定論的処理。
- 背景抑制: `applyAutomaticSubjectMask()` が外周色と中央優先度から重み付きマスクを作り、分析対象だけを絞る。
- 作品映像: `experience-prototype/art-assets/` のMP4とWebP。
- 保存: 撮影比較、マイデザイン、保存セットはブラウザの `localStorage` を使用する。
- 主要URLモード:
  - `?work=COLOR` など: 作品ページ
  - `?try=COLOR` など: 作品別固定試着
  - `?mode=custom`: 自由制作/SOURCE
  - `?mode=custom&from=COLOR`: 作品を引き継いだSOURCE

## 4. ディレクトリ構成

```text
tsuya-v2-deploy/
├── AGENTS.md                    # この制作継続ルール
├── PROJECT_STATUS.md            # 現在地点と次作業
├── README.md                    # 旧単体試着を中心に書かれた歴史資料。現状の正本ではない
├── index.html                   # 旧来の単体試着ページ。統合作品の本体ではない
├── top.html                     # 旧トップ系ファイル。現行導線の本体ではない
├── top-prototype/
│   └── index.html               # 現行の最初の入口。「艶」に触れて作品へ進む
├── experience-prototype/
│   ├── index.html               # 現行統合作品の本体。作品、試着、SOURCEを一括管理
│   ├── README.md                 # 試作24〜29の経緯。最新状態の正本はPROJECT_STATUS.md
│   ├── art-assets/               # 10作品のMP4/WebP等
│   ├── source-assets/            # SOURCE入口用の暫定動画
│   └── 試作*.md / 制作起点記録*.md
│                                 # 制作判断と作者性の記録
├── scripts/
│   └── encode_artwork_webp.py    # MP4をアニメーションWebPへ変換する補助スクリプト
└── 艶II_ローカルで開く.command   # localhost:8123でリポジトリルートを開く
```

`._*` と `.DS_Store` はSMB/macOS由来のゴミファイルである。読まない、編集しない、Gitへ追加しない。

## 5. 実装上のルール

1. 変更前に `experience-prototype/index.html` の既存構造を読み、必要最小限の差分にする。
2. MediaPipe、カメラ、ランドマーク平滑化、爪座標、描画ループをUI変更と同時にリファクタリングしない。
3. 作品を追加・変更するときは、`works` 定義、作品映像、作品専用ネイル、作品切替、固定試着、SOURCEからの戻り先を一組として確認する。
4. 作品タイトルや順序は `works` オブジェクトの順番が正本。2026-08-31時点では01〜10の10作品。
5. `BUILD_VERSION` と内部リンクのクエリを変更した場合は、作品一覧、作品ページ、試着、CUSTOM、BACK、HOMEの全導線を確認する。
6. SOURCEの生成規則は `sourceRecipes()` と `makeSourceSets()`、背景抑制は `applyAutomaticSubjectMask()`、分析は `extractSourcePalette()` と `analyzeSource()`、画像由来の再現可能な揺らぎは `sourceSeedFromPixels()` と `sourcePaletteTunings()` にある。
7. 写真分析の変更では、元画像の表示と分析用Canvasを混同しない。表示は元画像、マスクは分析専用とする。
8. UI変更後は必ず390×844相当で確認し、TRY ONやBACKが画面外・他要素の背後へ隠れていないか矩形位置も見る。
9. AppleDoubleのためGitが `non-monotonic index` 警告を出す場合がある。警告だけで失敗と判断せず、コミットIDとpush結果を確認する。
10. Gitへ追加するときは対象ファイルを明示し、`git add .` を使わない。`._*` を絶対にステージしない。
11. 公開確認はGitHub Pagesの反映待ちを考慮し、コミットIDをクエリへ付けてキャッシュを避ける。
12. 変更内容と実機確認結果を `PROJECT_STATUS.md` へ追記する。憶測は書かず、未確認は「要確認」とする。
13. `top-prototype/index.html` の退場演出コード `go()` は通常クリックへ接続済み。未使用のvariant分岐は残るため、存在だけを見て現行機能と判断せず、初期値とイベント接続を確認する。

## 6. 勝手に変更してはいけない部分

- MediaPipeのバージョンと読み込み元。
- `makeHands()`、`ensureLive()`、`updateLandmarks()`、`startCamera()`、`startLoops()`を中心とする追跡・カメラ系。
- 爪の座標計算、平滑化、片手追跡の性能調整値。
- 作品別の固定試着と自由制作を分離した構造。
- 自由制作で既に使える指別編集、手描き、画像柄、配色、質感、スカルプ、光、セット保存。
- 10作品の削除、統合、順序変更、タイトル変更。
- 採用済みの作品別5本セット、長さ、形、軌跡の強さ。
- 作品映像の明るさ、縦横比、`object-fit: contain`を使う作品ページの方針。
- SOURCE画像を外部へ送らない方針と、自動背景抑制。
- CUSTOMの明示的な戻る処理、HOME導線、タップ表示。
- ルートの旧ページや元動画を、整理目的で削除・上書きしない。

これらを変更する必要がある場合は、先に利用者へ変更理由と影響範囲を説明し、明示的な許可を得る。

## 7. テスト・確認方法

### ローカル起動

リポジトリ直下で次を実行する。

```bash
python3 -m http.server 8765 --bind 127.0.0.1
```

確認URL:

- `http://127.0.0.1:8765/top-prototype/`
- `http://127.0.0.1:8765/experience-prototype/`
- `http://127.0.0.1:8765/experience-prototype/?work=COLOR`
- `http://127.0.0.1:8765/experience-prototype/?try=COLOR`
- `http://127.0.0.1:8765/experience-prototype/?mode=custom`
- `http://127.0.0.1:8765/experience-prototype/?mode=custom&from=COLOR`

### 最低限の回帰確認

- 390×844のスマホ幅でトップから作品一覧へ進める。
- 10作品を切り替えられ、作品動画が自動再生される。
- 各作品から固定ネイル試着へ進み、その作品の5本セットが適用される。
- 固定試着ではカスタム操作パネルが出ない。
- 作品ページ右下からSOURCEへ入り、元作品の番号とネイルを引き継ぐ。
- 写真選択後、選択画像が右上に切れずに表示される。
- 写真から8色程度の特徴と、`COLOR / PATTERN / LIGHT / MIX`ごとの3案が生成される。
- 背景のある車・小物等の写真で、外周背景が以前より配色へ入りにくい。
- SOURCEのTRY ONが画面下で押せる。
- 試着開始後に操作パネルが`DESIGN`へ畳まれ、開閉できる。
- BACKが作品経由なら元作品、直接CUSTOMなら作品一覧へ戻る。
- HOMEが `top-prototype/` へ戻る。
- タップした場所に短いリング表示が出る。
- コンソールに新しいerror/warnがない。
- JavaScriptの文法検査と `git diff --check` を通す。

### 実機でのみ確認できる項目

- iPhone Safariでカメラ許可、背面／前面切替、手追跡、爪の追随。
- カメラの現在映像からSOURCEを生成する操作。
- 撮影、3秒タイマー、比較、写真保存。
- 実機での動画自動再生、画質、スクロール・固定UI、セーフエリア。

ブラウザ自動試験環境にカメラがない場合、成功したと書かず「要確認」とする。

## 8. 実装・公開の完了条件

利用者が「やって」または「公開して」と指示したアプリ変更は、ローカル実装だけで完了にしない。次を一連の作業として行う。

1. この `AGENTS.md` と `PROJECT_STATUS.md` を確認する。
2. 既存機能を壊さず実装する。
3. 390×844のスマホ表示を中心に動作確認する。
4. 変更した対象ファイルだけを明示してGitへ追加する。
5. コミットする。
6. `origin main` へpushする。
7. GitHub Pagesへの反映を公開URLで確認する。
8. キャッシュ回避用の版番号付き公開URLを利用者へ渡す。

このリポジトリのアプリ実装に伴うcommit、push、GitHub Pages公開は、利用者が「やって」または「公開して」と指示した時点で明示的に許可されたものとして扱う。「まだ公開しないで」と指示された場合だけローカルで止める。

改善版には `試作番号 / mobile番号` の連番を付ける。`BUILD_VERSION`、トップやHOMEを含む内部リンク、`PROJECT_STATUS.md` を同じ番号へ揃える。現在のローカル改善版は試作49 / `mobile49`。公開版は `PROJECT_STATUS.md` の冒頭を正本とする。

GitHub Pagesは常に `main` の最新版1つだけを現行公開版として扱う。過去版を別の公開先へ複製したり、旧版URLを利用者向けの選択肢として並べたりしない。公開報告では最新版の作品トップURLを先頭に1つ提示する。`?v=コミットID-mobile番号` はiPhone等のキャッシュを避けて同じ最新版を読み直すための値であり、過去版を残すURLではない。

「完成」と報告できるのは、GitHub Pagesの公開URLで反映を確認した後だけである。最終報告には必ず次を含める。

- 試作番号。
- コミットID。
- キャッシュ回避版番号付きのスマホ用公開URL。
- 確認した動作。
- iPhone実機でのみ確認できるため要確認の項目。
